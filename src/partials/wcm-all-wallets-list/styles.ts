import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listContentContainer: {
    paddingBottom: 12,
  },
  loaderContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
});
